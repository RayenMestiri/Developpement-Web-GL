# 📝 TP6 - Formulaires Template-Driven Angular# TP3Forms



Application de gestion d'étudiants avec **formulaire basé sur un modèle** (Template-Driven Forms).This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 20.3.6.



## 🚀 Démarrage## Development server



```bashTo start a local development server, run:

npm install

npm start```bash

```ng serve

```

Accès : **http://localhost:4200**

Once the server is running, open your browser and navigate to `http://localhost:4200/`. The application will automatically reload whenever you modify any of the source files.

## ✅ Fonctionnalités

## Code scaffolding

- ✓ **Liaison bidirectionnelle** avec `[(ngModel)]`

- ✓ **Validation en temps réel** : champs obligatoires, feedback visuelAngular CLI includes powerful code scaffolding tools. To generate a new component, run:

- ✓ **Messages d'erreur** contextuels

- ✓ **Barres colorées** : verte (valide), rouge (invalide)```bash

- ✓ **Bouton Submit** désactivé si formulaire invalideng generate component component-name

- ✓ **Deux zones** : formulaire ↔ résumé soumis```

- ✓ **Design moderne** : Bootstrap + gradients + animations

For a complete list of available schematics (such as `components`, `directives`, or `pipes`), run:

## 📚 Documentation complète

```bash

Voir [DOCUMENTATION.md](./DOCUMENTATION.md) pour l'implémentation détaillée de chaque section.ng generate --help

```

---

## Building

**TP6 - FST-Dep Info - Mohamed Lassoued**

To build the project run:

```bash
ng build
```

This will compile your project and store the build artifacts in the `dist/` directory. By default, the production build optimizes your application for performance and speed.

## Running unit tests

To execute unit tests with the [Karma](https://karma-runner.github.io) test runner, use the following command:

```bash
ng test
```

## Running end-to-end tests

For end-to-end (e2e) testing, run:

```bash
ng e2e
```

Angular CLI does not come with an end-to-end testing framework by default. You can choose one that suits your needs.

## Additional Resources

For more information on using the Angular CLI, including detailed command references, visit the [Angular CLI Overview and Command Reference](https://angular.dev/tools/cli) page.
